---

const { title, number } = Astro.props;

---
<div class="flex flex-col items-center justify-center p-4 bg-app-200 bg-opacity-10 rounded-4xl w-xs">
    <p class="text-2xl text-center font-semibold">{title}</p>
    <h4 class="text-3xl text-app-600">{number}</h4>
</div>
